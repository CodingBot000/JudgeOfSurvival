# Project Agent Instructions

- Respond in Korean by default unless the user asks for another language.
- After a successful commit and push, include the production Vercel link in the final response:
  `https://judge-of-survival.vercel.app/`
- If the user asks for the deployed link or Vercel link, provide:
  `https://judge-of-survival.vercel.app/`
- Do not claim that a newly pushed change has finished deploying on Vercel unless deployment status has been verified. If only the push was verified, state the production link and mention that Vercel may still be building the latest push.
